<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Surec baslarken uygulanan tema (sistem Karanlik Mod ACIK iken).
         Gerekceler values/styles.xml ile ayni; oyun her iki sistem temasinda da
         ayni uzay rengini kullanir. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/marsky_space</item>
    </style>
</resources>
